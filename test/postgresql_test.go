package test

import (
	"testing"
)

func TestPostgresql(t *testing.T) {
	/*db := postgresql.New("")
	err := db.Connect()
	if err != nil {
		t.Error(err)
		return
	}

	err = db.PingContext()
	if err != nil {
		t.Error(err)
		return
	}

	err = db.Disconnect()
	if err != nil {
		t.Error(err)
	}

	*/
}
